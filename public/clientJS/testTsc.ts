// testTypescript.ts

// A simple function to demonstrate TypeScript functionality
export function testTypescript(name: string, age: number): string {
    return `Hello Miss, ${name}! You are ${age} years old.`;
}